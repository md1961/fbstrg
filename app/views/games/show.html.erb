<table>
  <tr>
    <td><%= @game.is_ball_to_home ? '<>' : '' %>
    <td>H</td>
    <td class="numeric"><%= @game.score_home %></td>
    <td><%= '.' * @game.timeout_home %></td>
    <td><%= "#{@game.quarter}Q" %></td>
    <td><%= "#{@game.down}&#{@game.yard_to_go}" %></td>
  </tr>
  <tr>
    <td><%= @game.is_ball_to_home ? '' : '<>' %>
    <td>V</td>
    <td class="numeric"><%= @game.score_visitors %></td>
    <td><%= '.' * @game.timeout_visitors %></td>
    <td><%= time_left_display(@game.time_left) %></td>
    <td><%= ball_on_display(@game.ball_on) %></td>
  </tr>
</table>
<table>
  <tr>
    <td class="small"><%= @game.result %></td>
  </tr>
  <tr>
    <td class="small"><%= @game.offensive_play %></td>
  <tr>
  </tr>
    <td class="small"><%= @game.defensive_play %></td>
  </tr>
</table>

<br>
<%= form_tag @game, method: :patch do %>
  <%= text_field_tag :play %>
  <%= submit_tag :enter %>
  <%= content_tag :div, @game.error_message, class: :error %>
<% end -%>
